
# Numeric (integer and double)
num1 <- 10          # integer
num2 <- 3.14        # double

# Character (String)
char <- "Hello, R!"

# Logical (Boolean)
log_val <- TRUE

# Complex
complex_val <- 2+3i

# Factor (Categorical)
factor_val <- factor(c("apple", "banana", "apple", "orange"))

# Data Frame
data_frame_val <- data.frame(Name = c("Alice", "Bob"), Age = c(25, 30))

# List
list_val <- list(num1, char, log_val, data_frame_val)

